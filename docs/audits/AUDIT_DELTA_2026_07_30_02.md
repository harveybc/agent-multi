# Delta Audit — Closure Verification and Fork Classification

Audit ID: AUDIT-DELTA-20260730-02
Timestamp and timezone: 2026-07-30 22:44–22:50 America/Bogota (UTC-5)
Auditor: Satoshi
Requested by: user
Scope: independently verify Musashi's closures in
`CODEX_AUDIT_FINDING_CLOSURE_2026_07_30.md`; classify the competing-tip
warning Musashi explicitly left open; report all-front status, ETA and pending
work.
Excluded scope: no chain inspection beyond supervisor-reported tips; no chain
mutation proposed or performed; no tests executed; no broker action.

Evidence economy: this session consumed the **tier-0 collector packet**
(`~/.local/state/agent-multi/audit-snapshots/latest.json`, 22,483 bytes,
`meta.snapshot_sha256 = 845e55da19282e9e5bd213aefaefa718ae8e5bed9052790c85032ab7fc31b948`,
generated 2026-07-31T03:42:54Z) plus two targeted verification commands. This
is the first session to run on pre-collected evidence as designed.

## 1. Closure Verification

All four findings closed by Musashi are **independently confirmed**. The
auditor did not rely on the closure document alone.

| Finding | Musashi state | Satoshi verification | Verdict |
| --- | --- | --- | --- |
| AUD-F2-20260730-004 (S2) | verified_closed | `ibkr-paper-lab.sqlite` now written 22:42:55 (was frozen at 18:12); watchdog `ibkr` section now exposes `complete_sessions: 225` (advancing: 222 at closure), `latest_complete` with `reconciliation_observed_at`, and demotes `socket` to a sub-field instead of the availability signal | **Confirmed closed.** The correction implemented is exactly the requested one: functional freshness replaced port reachability |
| AUD-GEN-20260730-001 (S3) | verified_closed | Closure records doc 13 moved to `deployed_four_worker_running` | Confirmed (documentary) |
| AUD-F1-20260730-002 (S4) | verified_closed | Snapshot now emits `eta.fleet_candidates_per_hour = 1.7324` and `full_budget_remaining_seconds`, matching the revised measurement; doc 13 records the stage-1 decision point | **Confirmed closed**, and now machine-emitted rather than prose |
| AUD-GEN-20260730-003 (S4) | verified_closed | Recovery prompt at v1.1.0 with docs 08/11/14/16 | Confirmed (documentary) |

Auditor correction accepted: Musashi could not reproduce the `clientId 7
already in use` collision as a root cause; a fresh client ID produced the same
disclaimer error. That sub-hypothesis in my report is **withdrawn**. The
disclaimer was the sole root cause. My finding correctly identified the defect
and the watchdog masking, but overreached on the secondary mechanism.

Infrastructure delivered: `AUDIT-SNAPSHOT-COLLECTOR-001` is live at
`agent-multi@12d394ff` (six-hour timer, 28-pair retention, `CPUQuota=20%`,
`MemoryMax=256M`, no LLM). It resolves `OBS-20260730-C`. Verified in use this
session; section contract from file 03 is satisfied, including the `delta`
section with `changed_sections` and per-section hashes.

## 2. New Finding

### AUD-F1-20260730-005 — Equal-height chain fork persisting beyond the transient window

- Severity: **S3** (escalation criteria below)
- Confidence: high on observation; medium on mechanism (hypothesis flagged)
- Status: open
- Affected front: 1
- Repository/system: `doin-node` fork choice; campaign
  `phase-1-protected-execution-fleet-v2` job 0
- Observed evidence (snapshot 03:42:54Z):

  | Worker | Chain height | Tip | Finalized height | Finalized hash |
  | --- | --- | --- | --- | --- |
  | omega | 9 | `4b4f06a14156e699…` | 2 | `5218d3e0ef35422c…` |
  | gamma-5070ti | 9 | `4b4f06a14156e699…` | 2 | `5218d3e0ef35422c…` |
  | gamma-5090 | 9 | `4b4f06a14156e699…` | 2 | `5218d3e0ef35422c…` |
  | **dragon** | 9 | **`603dfe1a086d56fd…`** | 2 | `5218d3e0ef35422c…` |

  All three supervisors raise `swarm_health` warning "workers report competing
  blockchain tips at the same height", first seen 2026-07-31T01:26:41–46Z,
  still active at 03:42:54Z — a persistence of **~2 h 16 min**.
- Inference: Dragon is a 1-versus-3 minority on an **unfinalized** tip at
  height 9. The finalized anchor is identical on all four workers, and the
  shared population (generation, fingerprint) and best fitness
  (`0.00048223070314018903`) agree fleet-wide, so this is **not** a parallel
  lineage and **not** chain corruption.
- Contract tension: document 05 section 10.1 states equal-height tips "are
  resolved through DOIN's existing heaviest-chain `ForkChoiceRule`, including
  its deterministic tie-break… a node must not wait for one branch to become
  longer before converging." Document 09 section 7 treats same-height
  divergence as "an alert until every participant reports one tip." A
  two-hour-plus unresolved split is therefore longer than the declared
  behavior implies, though document 15 section 3.2 does accept a bounded
  equal-height terminal fork at the completion barrier when the finalized
  anchor matches — which it does.
- Business impact: currently none observed. The bounded risk is that if Dragon
  authored an `optimae_accepted` or `candidate_evaluated` transaction that
  exists only on its branch, a later reorganization could orphan that record,
  losing on-chain evidence for work its GPU actually performed. Champion
  fitness currently agrees fleet-wide, so no champion appears stranded now.
- Technical impact: the completion barrier for job 0 requires one tip or one
  verified finalized anchor; the finalized-anchor path remains available, so
  deadlock at convergence is unlikely but not proven.
- Hypothesis requiring a named test (not asserted): the deterministic
  tie-break is either not being invoked on equal-height announcements, or
  Dragon's branch is not being re-announced to it.
- Minimal reproduction: read `runtime.participants.*.workers.*.tip_hash` and
  `finalized_hash` from any audit snapshot, or `curl` each supervisor
  `/api/status`.
- Proposed correction: **none yet — do not mutate the chain.** Run the bounded
  read-only test below first. Musashi's judgment that stronger evidence is
  required before any chain action is endorsed.
- Named test (read-only, proposed as task AT-F1-011):
  1. Retrieve block 9 from Dragon and from a majority node; compare generator,
     transaction IDs and types.
  2. Determine whether either branch carries transactions absent from the
     other, specifically `optimae_accepted` or `candidate_evaluated`.
  3. Read the `ForkChoiceRule` tie-break in `doin-core` and evaluate which tip
     it should select for these two hashes at equal height.
  4. Re-sample after the next finalization advance; a fork that dissolves when
     finalization passes height 9 is benign convergence latency.
- Escalation criteria to S2: the split survives a generation boundary, or
  step 2 shows Dragon-only accepted transactions, or the finalized anchors
  diverge.
- Required regression or monitor: an alert that distinguishes *transient*
  equal-height divergence from one persisting beyond a declared threshold, and
  that reports whether the minority branch holds unique accepted transactions.
  The current warning is binary and cannot express that difference.
- Owner: Musashi (classification and any repair); Satoshi (the read-only test)
- Dependencies: none

## 3. New Observation

**OBS-20260730-D — Gamma is the resource-constrained host.** From the snapshot
`machines` section: gamma has 15.34 GB RAM total with 6.12 GB available, swap
1.86 GB free of 4.29 GB (roughly 2.4 GB in use), 50.66 GB free disk of 411 GB
(12 %), and a campaign-cgroup `sock_throttled` counter of **6,228** while omega
and dragon report 0. OOM kills are 0 on all hosts and both Gamma GPUs are
healthy (5070 Ti 49 °C/33 %, 5090 56 °C/54 %). Document 13 already flags Gamma
memory as requiring observation and document 17 restricts large jobs from
gamma-5070ti. Not a finding: no OOM, no stall, no throughput anomaly. Verify in
the next delta whether `sock_throttled` grows and whether swap use trends up.

## 4. All-Front Status

### Front 1 — optimization and research: HEALTHY, PROGRESSING

- Job 0 `usdcad-4h-protected-easy-sac-shared-v2`: **56 of 480 candidates
  (11.7 %)**, generation 2 with 16/20 evaluated, stage 1/4
  `data_observation`, chain height 9, best fitness `0.00048223070314018903`.
- Fleet throughput **1.7324 candidates/hour**; snapshot-computed remaining
  **881,079 s = 10.2 days** on the full budget, explicitly before early
  stopping.
- Lineage: one dataset SHA `f2fa13f4ab9d…`, one genesis `4e19257e8941…`,
  identical finalized anchor, four distinct claims.
- One open item: AUD-F1-20260730-005 above.

### Front 2 — execution reality: RECOVERED, PARTIALLY BLOCKED

- Alpaca Paper: healthy, sessions advancing, 0 positions/orders,
  `protected_execution_eligible: false` (correct).
- IBKR Paper: **recovered**, 225 complete reconciled sessions, 0/0 exposure,
  five-minute cadence restored, watchdog now functionally aware.
- OANDA MT5: blocked, `mt5_bridge_missing` — VM commissioning incomplete.
- OANDA Practice REST: `not_configured`, permanent for this account division.

### Front 3 — social intelligence and continuity: DORMANT, CORRECTLY BOUNDED

Verified 2026-07-30 (section 7c of `AUDIT_STATUS_2026_07_30.md`): no social
collector or publisher jobs exist; `ollama serve` holds no GPU memory;
`hermes-gateway.service` is the only enabled Hermes unit; the active Hermes
job enforces `can_place_orders/can_change_risk/can_enqueue_optimization =
false` with `requires_human_review = true`. No change detected this session.

## 5. Commands and Queries

Tier-0 pre-collected (no Satoshi cost): campaign runtime, ETA, lineage, all
three hosts and four GPUs, memory/swap/disk/OOM counters, broker observer
state, watchdog state, 11-repository provenance — all from the collector
packet. Satoshi-collected this session: `stat` on two venue databases and a
read of the broker watchdog `latest.json` (IBKR closure verification), plus
`ls` of the snapshot directory. Delegation ratio is now strongly in favour of
tier 0, as intended.

## 6. Next Audit Trigger

Next scheduled: AT-F1-001 (protected-entry v2 contract verification) at the
24-hour delta slot, unless the fork classification (AT-F1-011) is prioritised
first — recommended, since it is cheap and time-sensitive while the fork is
observable. Event triggers unchanged: stage transition, convergence, champion
archive, MT5 activation, canary enablement, incident.

## 7. Change Confirmation

No code, configuration, service, campaign, chain, broker, credential or Git
state was modified. No chain repair was proposed or attempted. Writes were
limited to `docs/audits/` and the register.
