# Audit: DATA-SOTA-364..368 Return

Date: 2026-08-27 America/Bogota
Auditor: General Musashi
Reviewed tip: `agent-multi@588476b5`

## Verdict

**364-367 ACCEPTED AS CORRECTIONS. 368 CONFIRMED. FULL5 GENERATION REJECTED
FOR THE PAIRED GPU SCREEN.**

Independent focused reproduction: **48 passed**. The C4 predeclaration commit
precedes the calibration implementation and evidence. No paired GPU driver or
launched run exists.

## Accepted Facts

- Barrier targets use aligned OHLC first-touch facts; wick/gap hits and
  adverse-first same-bar collisions are executable, and close-only use refuses.
- Mechanics authority comes from a frozen train-tail probe. Monitor mutation
  cannot change mechanics eligibility.
- Barrier support is checked per horizon using calibration only; h6 and h12
  both pass the predeclared support floor for o2022.
- The paired materializer now binds a concrete generation seal, contract, data,
  preprocessing and per-family encoder digests rather than prose.
- The conflict run was fixed prospectively at 8 epochs, 2,400 windows, equal
  probes and solo-versus-joint comparisons.

## Scientific Disposition

The five-objective generation sealed as `ea950ecb...` is valid evidence of the
failure mode, but it is not an eligible treatment artifact. Four of five
families show material degradation under joint training. Sending it into SAC
would confound the question "does pretraining help?" with "does this known-bad
multi-task optimizer damage the encoder?"

Contrastive is the most visible aggressor, but deleting it is not yet justified:

- oscillators did not show material degradation;
- returns/momentum and trend/level have conflicts not caused by contrastive;
- the observed mechanism combines gradient direction conflict and highly
  unequal effective gradient magnitude.

The next experiment must therefore isolate the multi-task optimization
mechanism before changing the objective set.

## Residual Notes

- The barrier target now matches intrabar trigger semantics. Its volatility
  scale and multipliers remain a representation-learning target, not a claim of
  exact equality with every live envelope; documentation must say so.
- Eight epochs establish this specific conflict result, not universal objective
  incompatibility.
- The full5 seal and its 12 genesis records remain preserved as rejected
  evidence and must never be silently relabelled as the corrected treatment.

