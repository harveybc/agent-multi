## WP21(b) — the environment's own training reward, three arms, one seal

Read this table with its first two sentences or not at all. environment_return_training_reward_total is the gym-fx environment's own reward under its configured reward plugin, summed over the replayed steps. It is a training signal inside a simulation with the campaign's own commission, slippage and execution rules; it is not profit, not a backtest result and not evidence about any market.

`policy_profitability` stays refused by `m5phet_evaluation`, and the closure table generated by `evaluation/compare_stages.py` therefore carries NO_NEW_MEASUREMENT for every row of this area. This table exists to put the totals the reports do carry beside each other on the rows one seal covers; it is not a quality claim and no rank in it is evidence about any market.

Seal `f6b715d022b907d42067c3df14a22a457a9cefb584b778ea1cd9f9284b778efc` over 256 scored rows; protocol `65013b74c2a57f6f10586eb7104ad9a31c1ae10505c60d9fdf104d63efee8844`.

| stage | status | metric | scale | model value | naive reference | naive value | difference from naive | skill | literature value | literature source | comparability | rank |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| fitted_sac | MEASURED | environment_return_training_reward_total | the environment's own reward units under its configured reward plugin, summed over the replayed steps; they are units of a training signal and convert to nothing outside this simulation | -0.003888 | flat | 0.000000 | -0.003888 | NOT_DEFINED | NOT_CARRIED | NOT_CARRIED | COMPARABLE | 3 |
| flat | MEASURED | environment_return_training_reward_total | the environment's own reward units under its configured reward plugin, summed over the replayed steps; they are units of a training signal and convert to nothing outside this simulation | 0.000000 | itself (this row IS the naive reference) | 0.000000 | 0.000000 | NOT_DEFINED | NOT_CARRIED | NOT_CARRIED | COMPARABLE | 1 |
| laya_first_layer | MEASURED | environment_return_training_reward_total | the environment's own reward units under its configured reward plugin, summed over the replayed steps; they are units of a training signal and convert to nothing outside this simulation | 0.000000 | flat | 0.000000 | 0.000000 | NOT_DEFINED | NOT_CARRIED | NOT_CARRIED | COMPARABLE | 1 |

Skill column, in full: NOT_DEFINED: skill is 1 - model_error / naive_error on the same rows, and this quantity is a signed score, not an error; against a naive total of 0.0 the ratio is undefined and against any other it would not mean skill

Literature column, in full: no literature value is carried: the quantity reported here is one environment configuration's own training reward, which no published result is expressed in.
