# HEURISTIC-001 Local Acceptance

Date: 2026-07-11
Repository: `/home/harveybc/Documents/GitHub/heuristic-strategy`
Base commit: `51d4a0fda973314e29e81ff664cfcdf27139f4e4`
Decision: **verified locally**
Commit/deploy status: not committed, not pushed, not deployed

## Accepted Scope

- pure `DecisionContext -> AssetIntent` prediction entry/exit policy;
- validated policy parameters and optimizable schema;
- legacy entry geometry, A-G early-close logic, and order-size interpolation;
- Backtrader adapter delegation to the pure calculations;
- normalized forecast-path adapters for ideal/mapped, CSV, direct callback,
  and provider callback sources;
- source-independent `PredictionBundle` construction;
- frozen real-Backtrader requested-action replay;
- packaging entry point `prediction_entry_exit_v1`.

## Frozen Legacy Replay

Fixture:

```text
tests/data/prediction_entry_exit_backtrader_replay_v1.json
```

The fixture was generated by executing the strategy implementation from base
commit `51d4a0f` on a deterministic bar and prediction stream. Before freezing,
the same harness was run against the refactored adapter and produced an exact
event match.

The replay covers:

- long entry with size and SL/TP geometry;
- prediction-driven early close;
- a second long entry and TP close;
- short entry and TP close;
- flat/no-action bars between completed orders.

It freezes policy decisions and requested orders. Fill price, commission, P&L,
and simulator accounting parity remain Phase 3 responsibilities.

## Source Substitution

Four adapters produced identical short/long paths:

- ideal/mapped fixture source;
- legacy CSV source;
- direct-model callback;
- provider-API callback.

After conversion to `PredictionBundle`, all four produced the same action,
target exposure, strategy relative volume, stop, take profit, and reason code.
Transport and HTTP behavior are intentionally outside the pure policy core.

## Verification

```text
python -m pytest -q \
  tests/unit_tests/test_prediction_entry_exit_policy.py \
  tests/unit_tests/test_prediction_entry_exit_backtrader_replay.py \
  tests/unit_tests/test_prediction_source_substitution.py

17 passed
```

Additional checks:

```text
python -m compileall -q app/policies ...
git diff --check
python -m pip wheel . --no-deps ...
```

The wheel contains both policy modules and the
`trade_lifecycle_policy.plugins` entry point. Importing `app.policies` does not
load Backtrader.

## Historical Test Debt

The repository-wide suite was already invalid at the base commit. Current
collection encounters stale tests for removed configuration, data processing,
plugin-loading, and generic prediction-client APIs, plus an embedded
`timeseries-gan` test tree. A bounded run also reproduces eight stale API test
failures. These are not counted as passing and were not suppressed to claim a
green global suite.

This debt is separate from the accepted lifecycle-policy contract. A future
repository-maintenance task should classify or migrate those legacy tests and
define top-level test discovery independently from embedded repositories.
