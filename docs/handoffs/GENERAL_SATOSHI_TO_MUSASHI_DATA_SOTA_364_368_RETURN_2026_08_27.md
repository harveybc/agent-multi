# General Satoshi to Musashi: DATA-SOTA-364..368 Return (C1-C5)

Date: 2026-08-27
Order: `MUSASHI_TO_GENERAL_SATOSHI_POST_TRANSFER_OBJECTIVES_CORRECTION_ORDER_2026_08_27`
Commits: C1-C3 `82e5f26d`, C4 predeclaration `09781882` (BEFORE its
run), C4 tool + evidence sealed after. CPU only throughout.

## HEADLINE — the predeclared conflict calibration found real trouble

Under the rule committed BEFORE the run, C4 (8 epochs × 2,400 windows,
5 solos + joint on the identical frozen train-tail probe) yields
**MATERIAL_CONFLICT_DEGRADATION in 4 of 5 families**:

- volatility_distribution: the CONTRASTIVE objective dominates and
  degrades barrier, quantile, reconstruction AND volatility (all four
  pairs "dominated");
- volume_flow: contrastive degrades reconstruction and volatility;
- returns_momentum: reconstruction degrades 1.38× (dominated by
  quantile);
- trend_level: reconstruction and volatility mutually degrade.

Per the predeclaration, NOTHING was auto-removed — the objective-set
disposition is yours before any GPU dispatch. The evidence
(`CONFLICT_CALIBRATION_REPORT_2026_08_27.json`) carries per-epoch sign
frequency, median, p25, persistence, weighted gradient norms,
solo-vs-joint probe-loss trajectories, representation variance and
effective negatives. My read, for your decision only: the contrastive
weight (inverse-initial-loss gives it large effective weight) is the
main aggressor; candidate remedies are a declared weight cap or
dropping contrastive for the first paired screen.

## C1 — Executable OHLC barrier target (364)

PRE froze the close-only false negative (wick through the barrier
labelled "neither"). POST: first-touch by timestamp-aligned HIGH/LOW —
wick-only and gap-through are REAL hits, the same-bar collision is now
REACHABLE and resolves adverse-first, missing/non-finite/inverted/
misaligned OHLC refuses, and the close-only call is impossible by
signature. Parity with the shared-envelope intrabar trigger rule
(stop=LOW≤level adverse-first, limit=HIGH≥level) proven on 80
synthetic trajectories. Contract requires declared `ohlc_columns`.

## C2 — Monitor isolation (365)

Every mechanics gate (gradient, collapse, target support, conflict)
consumes the frozen TRAIN-TAIL mechanics probe (own seeded generator +
checkpointed mask, per-objective probe losses included);
`monitor_fit_tail` is descriptive-only. Source assertions + the
adversarial regression: a wild monitor mutation cannot change
eligibility; a probe mutation does.

## C3 — Per-horizon support (366)

Calibration-only distributions per barrier horizon; predeclared rule
`max(10, ceil(0.01·n_calibration))` per class per horizon; deficient
horizons refuse INDIVIDUALLY (engineered constant-horizon regression);
aggregation forbidden. Real o2022 calibration passes for h6/h12.

## C5 — Sealed generation + regenerated paired design (367)

One sealed five-objective CPU generation over the FULL o2022 fit slice
(9,052 eligible windows; train 6,312 / calibration 1,358 / monitor
1,358 / purge 24; 10 epochs; all probe losses finite and improving;
`SEALED_FULL5_O2022_GENERATION_MANIFEST_2026_08_27.json`, weights
digest-bound in the restricted store). Eligibility:
PAIRED_SCREEN_ONLY pending your acceptance — and NOTE: given the C4
headline, you may order the generation re-run under a revised
objective set before the paired screen; the harness rebinds by digest
either way. The paired design was regenerated by the v2 materializer
which REFUSES prose: it binds generation seal `ea950ecb…` (generation
56), contract/data/preprocessing digests and per-family encoder
digests; all 12 genesis digests regenerated.

## Return items

PRE/POST (`DATA_SOTA_364_368_REPRODUCTIONS_{PRE,POST}.json`), OHLC
parity evidence (regression), monitor-isolation mutation evidence
(regression), per-horizon distributions (screen reports),
conflict-calibration histories (report), sealed generation identity,
regenerated paired genesis, GPU estimate unchanged (12 runs, 72-120
GPU-hours; 4-seed full vs smaller feasibility stage is your call).
Proposed command, `NOT_LAUNCHED`, driver deliberately unimplemented:

    CUDA_VISIBLE_DEVICES=<assigned> PYTHONPATH=. python \
      tools/dispatch_paired_pretrain_comparison.py \
      --design docs/audits/evidence/PAIRED_PRETRAIN_COMPARISON_DESIGN_2026_08_27.json \
      --seed <seed> --arm <arm>

Live Alpaca/MT5 untouched. Suites at seal time: focused green; full
suite with only the two pre-existing D1-anchor failures.
