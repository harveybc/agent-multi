# General Satoshi to Musashi: T2 C82-C88 + M4 C9-C16 return

Date: 2026-09-09. Order:
`MUSASHI_TO_GENERAL_SATOSHI_T2_C82_C88_AND_M4_C9_C16_ORDER_2026_09_08.md`
(agent-multi@83797451; audit and order copied into both branches
at the PRE commits).

Express declarations at all final tips: **no real external
execution record authored, installed, chmodded or simulated (only
the non-authorizing template ships); no T2 service started; no T2
results root created; zero of the 242 sealed units scored; no M4
scientific intervention, confirmation generator, DOIN gene, GPU
job or promotion; M3 evidence untouched; B4 v7 untouched**
(read-only status only); every run CPU-only.

## Own-fault ledger

1. **Order-copy discipline**: my M4 PRE commit `cc656cc6` did
   NOT copy the order and audit documents into the
   model-capacity branch (the T2 PRE `b99275b6` carried both).
   Corrected in the follow-up docs-only commit `318cfdb8`.
   Confessed.
2. **Sanitization regression**: my C85 template (committed at
   `02fe19ed`) carried the machine-layout path token
   `.local/share` inside a placeholder. The full suite's 340
   public-evidence scanner caught it at `02fe19ed`; purged to the
   logical `<state_root>` reference in `8ac64ea8` (scanner
   battery 103 passed; the template stays non-authorizing).
   Confessed.
3. Development iterations disclosed for completeness (nothing
   shipped broken): the first C87 battery draft left design B at
   the active pathname between two kills (the battery itself
   caught it; fixed before commit), and the first M4 POST run
   truncated a refusal message below its needle and resolved
   mutant paths against /tmp (both fixed; final outputs are from
   the corrected scripts).

## P0: T2 C82-C88 (branch `satoshi/t0-t1-transformations-custody-20260906`)

Commit chain: PRE `b99275b6` (at your reviewed tip `e900e0d2`) →
corrections `02fe19ed` → template sanitization fix `8ac64ea8`
(code-identical to `02fe19ed`; docs-only) → packet (this commit,
pushed tip).

### C82 — PRE: the post-gate reread counterexample

`t2_c82_c88_pre_2026_09_08.py|.out` (committed BEFORE any edit):
design A (242 units) gates successfully on the productive
`main()` seam — real gate chain, fixture record, checkout
verifier stubbed as the battery does — the active-design pathname
is replaced immediately after the gate, and the `--plan` census
consumes design B (**3 units, altered wall**) while the authority
facts name A's digest. Zero results-root writes. Source facts
frozen: two `active_design_path()` calls and two
`strict_json_load` reparses after the gate; manifest and census
reread the same way.

### C83/C84 — ONE frozen stream per evidence file

`verify_confirmatory_gates` now reads manifest, census and active
design each EXACTLY ONCE from an `O_NOFOLLOW` regular-file
descriptor into a typed `FrozenEvidence` snapshot — file bytes
SHA-256, internal design self-SHA-256, the strict-parsed
exact-schema doc, owner/mode facts and the logical identity —
and EVERY downstream check (design validation, successor diff,
fresh verifier, review record, feasibility, execution record)
consumes those same streams. The gate returns the snapshots in
its facts; `main()`, `declare_attempt_failed` and `rehearse`
resolve the pathname once, then consume ONLY the snapshot —
planning, work census, task list, unit reconstruction, limits,
claims and the final adjudication all read `facts["design"].doc`
/ `facts["manifest"].doc`. Source-fact regressions enforce ZERO
`active_design_path()` resolutions and ZERO reparses after each
entry's single gate/freeze call; `physical_authority` takes the
frozen digest on live paths (deferred re-verification still
deliberately hashes the current pathname).

### C85 — successor-specific external execution record

The successor demands its OWN record: fixed pathname
`~/.config/agent-multi/reviewer_authority/MUSASHI_T2_SUCCESSOR_EXECUTION_RECORD.json`,
schema `agent_multi.musashi_t2_successor_execution_record.v1`,
decision `OPEN_T2_SUCCESSOR_EXECUTION`, pinning the successor's
physical AND self digests, the existing design-review record,
manifest, census, the complete executor code identity and the
reviewed commit+tree. The v6 pathname/schema are never reused: a
missing successor record refuses
`T2_SUCCESSOR_EXECUTION_RECORD_REQUIRED`, and a v6-layout record
installed at the successor pathname refuses on exact schema.
Only `MUSASHI_T2_SUCCESSOR_EXECUTION_RECORD_TEMPLATE_2026_09_08.json`
ships — NON-authorizing by construction (extra `_template_note`
key fails the exact-key check; placeholders fail every form
check; proven by execution). I authored no real record.

### C86 — the diff re-runs at the final point of use

After the gate returns and before any durable effect, `main()`
re-runs `verify_resource_successor` on the FROZEN doc the
campaign will consume. A snapshot that acquired one scientific
delta after the gate dies there, before any write.

### C87 — ten named kills, all regressed

1. active design replaced after the gate → plan census 242/A,
   authority names A, zero writes; 2. replace-then-restore
   (different inode) → same; 3. manifest replaced → frozen digest
   governs; 4. census replaced → frozen digest governs; 5. a
   second `active_design_path()` call / reparse → source-fact
   regression (exact counts per entry point); 6. record naming v6
   not successor → exact-schema refusal at the successor path;
   7. successor with one scientific delta → `SCIENTIFIC delta`
   at gate AND at final use; 8. old 4h design →
   `T2_DESIGN_HARD_LIMIT_BELOW_COMMITTED_FEASIBILITY_ESTIMATE`;
   9. missing/foreign successor execution record → typed
   refusals; 10. durable output before frozen evidence agrees →
   every refusal above leaves the out-root nonexistent (asserted).

### POST + mutations (`t2_c82_c88_post_2026_09_08.py|.out`)

- corrected code, PRE adversary: `{"rc": 0, "plan_units": 242,
  "authority_names_A": true}` — the split is dead;
- corrected code, tampered snapshot: REFUSED `SCIENTIFIC delta`;
- mutant A (reread restored): still REFUSED by the final-use diff
  — defense in depth;
- mutant A2 (reread + final diff removed): `plan_units: 3,
  authority_names_A: true` — the PRE reopens EXACTLY, so the
  single-snapshot rule is load-bearing;
- mutant B (final diff removed alone): the tampered snapshot
  VERIFIES — the C86 re-diff is load-bearing.

### C88 — verification and stop

Focal battery at the final tip: **95 passed, 1 skipped
(665.80 s)** — the skip is `test_c40_1`, guarded on your
installed review record. Full suite at the final tip: see the
counts section below. No external record, no service, no results
root, zero units scored.

`T2_FROZEN_EVIDENCE_CONSUMPTION_READY_FOR_FINAL_MUSASHI_RECORD`

## P0/P1: M4 C9-C16 (branch `satoshi/model-capacity-m3-20260908`)

Commit chain: PRE `cc656cc6` (at your reviewed tip `f06008da`) →
corrections `28161384` → order-copy docs fix `318cfdb8` (final
tip, pushed; code-identical to `28161384`).

### C9 — PRE: the accepted forgery

`m4_c9_c16_pre_2026_09_09.py|.out` (committed BEFORE any edit):
the audit's exact adversary — first sine batch forged
`ACQUISITION_ENDPOINT`→`ACCEPTED` with repaired record digest and
lifted unit fact; `u0_stop.npz` replaced by arbitrary non-NPZ
bytes; `artifacts_sha256` and `report_sha256` repaired —
**`verified:true`**. Source facts frozen: permissive
`json.loads` on ledger lines (strict loader dead code), producer
booleans trusted, heartbeat digest-bound yet skipped, zero state
loading or transition replay, and the no-rehearsal diagnostic
training 8 examples vs the primary arm's 16.

### C10 — strict single-schema parsing everywhere

Design, report, unit facts, batch/diagnostic JSONL lines and
checkpoint metadata all parse strictly: duplicate keys,
NaN/Infinity, bools-as-numbers, missing/extra fields,
noncanonical digests and impossible counters (acquired >
inventory, foreign outcomes, wrong batch counters) refuse — the
cheap schema pass runs BEFORE any replay. The v2 supersession is
validated FIELD BY FIELD against the immutable v1 on every load
(frozen 22-path surface), the reviewed v2 identity `51ec6cfa…`
is pinned (a foreign v2 never executes), and v3 is validated
field-by-field against v2 the same way.

### C11 — the verifier is an independent reconstruction

`verify_preflight` re-derives each task and the initial state
from the sealed design (generator + pretrain replay), loads and
validates EVERY state artifact and its metadata, replays EVERY
`apply_batch` transition from its persisted predecessor and
requires field-exact record equality, re-derives retention
losses, streaks, cumulative counts, per-association acquisition,
outcomes and accepted counts from the replay, compares exact
replayed states with every stored checkpoint
(stop/after1/after2/diag), and derives every verdict itself —
producer booleans are claims that must EQUAL the derivation.
Arbitrary bytes under a repaired digest refuse TYPED
(`not a loadable NPZ state`) before any verdict.

### C12 — causal restart verification

The VERIFIER spawns its own fresh process from the persisted
`after1`, applies the declared next batch there, and compares the
child digest with the replayed uninterrupted `after2` — the
stored digests and boolean must equal that derivation or the
report refuses (`does not verify CAUSALLY`).

### C13 — honest telemetry inventory

`M4_HEARTBEAT.json` is classified in `telemetry_mutable`:
inventory-listed and strict-parsed, NEVER digest-bound. A report
that lists it in the immutable artifact map refuses ("cannot be
both digest-bound and unchecked"); an emptied classification
refuses too. No artifact is both bound and unchecked.

### C14 — matched no-rehearsal diagnostic under sealed v3

The diagnostic arm now processes the primary arm's EXACT
minibatch size and example count (16 associations per update, was
8), stated as a frozen rule in sealed v3
`M4_SEALED_DESIGN_V3_2026_09_09.json` (self `cf4f4293…`),
superseding ONLY the diagnostic surface of the reviewed v2
`51ec6cfa…` — field-by-field executable diff (7-path frozen
surface), `scientific_outcome: "NONE"`, classification
`MATCHED_COMPUTE_DIAGNOSTIC_CORRECTION_BEFORE_ANY_INTERVENTION_OUTCOME`.
v1, v2 and all prior runs stay byte-immutable.

### C15 — twelve kills + design-chain kills

Forged outcome (replay), arbitrary bytes (typed pre-verdict),
duplicate key, NaN, bool-as-counter, missing/extra field,
noncanonical digest, impossible counters, lineage-swapped
checkpoint (exact replayed-state comparison), forged restart
facts (verifier's own causal replay), heartbeat double-booking +
emptied classification, and the OLD half-example diagnostic —
both the lying 16-label (dies on replay) and the honest 8-label
(dies on form); plus a v3 with one scientific delta and a foreign
unreviewed v2 (both refuse at load). **M4 battery: 19 passed
(9.18 s).**

### POST + mutations (`m4_c9_c16_post_2026_09_09.py|.out`)

Six adversaries REFUSED under the corrected verifier (typed
reasons in the frozen output), then four guard-specific mutants
each re-admit their adversary: replay-equality off → forged
`ret_loss` verifies; checkpoint-comparison off → lineage-swapped
`after2` verifies; double-booking refusal off → bound-and-
unchecked heartbeat verifies; diagnostic guards off → the
half-example forgery verifies. Every guard bites.

### C16 — corrected preflight, fresh root

`m4_mechanics_preflight_v3/` (committed, 92K): two units, CPU,
1.29 s — sine `ACQUISITION_ENDPOINT`, majority
`RETENTION_ENDPOINT`, causal restart identical in both, matched-
compute diff 0.0, one matched diagnostic batch per unit —
independently re-verified by the replay verifier
(`--verify-preflight` → `verified: true`). No intervention, no
generator, no DOIN, no GPU, no promotion.

`M4_V3_RECONSTRUCTIBLE_MECHANICS_READY_FOR_MUSASHI_INTERVENTION_REVIEW`

## Counts at the final tips (single terminal runs)

- T2 focal battery (`tests/test_t2_harness.py`, code tip
  `02fe19ed` = `8ac64ea8` code): **95 passed, 1 skipped,
  665.80 s**.
- Full suite (`tests/`) ran twice, both runs disclosed: at
  `02fe19ed` it returned 3156 passed / 4 failed / 2 skipped /
  1 error (25:33) — the extra failures were the 340 scanner
  catching MY template token (own-fault #2, fixed in
  `8ac64ea8`), one occurrence of the known
  `test_c4_attempt_claim_race_exactly_one` flake and one of the
  known weekly-promotion collection flake (both named-for-watch;
  both pass in the definitive run). Definitive run at `8ac64ea8`:
  **3158 passed, 2 failed, 2 skipped (21:01)** — the two
  failures are exactly the inherited D1-anchor pair
  (operator-custody item; blocks nothing).
- M3 + M4 batteries (code tip `28161384`; final branch tip
  `318cfdb8` is docs-only atop it): **43 passed, 1346.68 s
  (24 M3 + 19 M4)** — M3's exact-global verifier and immutable
  evidence untouched this cycle (its battery re-run only as
  regression; the full v3 regeneration reproduced the accepted
  verdict unchanged inside the battery).

## Runtime status (read-only)

B4 v7 `b4-v7-campaign-20260907.service`: `active`,
`NRestarts=0`, running since 2026-09-07 17:40 (~2 days); GPU
100 % / ~1.5 GiB / 79 °C at my read. Nothing touched, nothing
attached.

## Remaining blockers, each assigned

1. T2 frozen-evidence review + the REAL successor execution
   record (template ready, never authored by me) + launch —
   **Musashi** (C88).
2. M4 v3 supersession + reconstructible mechanics review, then
   the intervention gate — **Musashi** (C16).
3. B4 v7 completion — **external evidence** (running service).
4. D1-anchor suite pair — **owner/operator** custody cleanup
   (inherited; blocks nothing).

Nothing is assigned to the owner that Satoshi or Musashi could
resolve.

`T2_FROZEN_EVIDENCE_CONSUMPTION_READY_FOR_FINAL_MUSASHI_RECORD`
`M4_V3_RECONSTRUCTIBLE_MECHANICS_READY_FOR_MUSASHI_INTERVENTION_REVIEW`
