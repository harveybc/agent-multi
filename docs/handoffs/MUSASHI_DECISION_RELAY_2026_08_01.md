# Decision Relay — Objective Contract: Alternative A

Date: 2026-08-01
Author: Satoshi, relaying the owner's decision
Recipient: General Musashi
Authority: Harvey, owner-ratified ("go with A"), recorded in
`docs/audits/work_plan/04_OPEN_FINDINGS_REGISTER.md` section 1f

Musashi — the objective decision has landed. Facts and consequences, all
labeled:

1. `owner-ratified`: **Alternative A.** Job 0 completes unchanged; its
   champion is initialization evidence under the declared full-period proxy.
   **Job 1 is the authoritative selection** under `robust_weekly_rap_fitness`.
   No mid-chain mutation — the position we both held is now decree.
2. `observed`: your rider test (`agent-multi@06de651f`) already satisfies
   rider (i); nothing more needed there.
3. `proposed`, yours at or before archive time — rider (ii): relabel the
   job-0 champion "alpha handoff under full-period proxy objective" in every
   artifact and status surface, so no downstream reader mistakes it for a
   weekly-validated performance claim.
4. `proposed`, mine at job-0 completion — AT-F1-013: re-verify against the
   final chain that the elite warm-start set contains the weekly top-2
   candidates. Neither of us pre-claims it; the event triggers the audit.
5. Finding 026 → verified_closed by owner decision; AT-F1-001 → closed;
   Alternative C retired as falsified; B expired unexercised.
6. Related owner-driven items from register 1e, for your queue when
   convenient: add **USDCAD** (and doc-21 FX universe) to the MT5 EA watch
   list — the optimized asset currently has no live cost stream; and the
   observed→scenario cost-calibration pipe (new domain hashes at job
   boundaries only) awaits a packet if you take it.

The swarm needs nothing from either of us. Fight well.

— Satoshi
