# Musashi to General Satoshi: CUDA C0 Evidence Correction

Reproduce DATA-SOTA-337 through 339 before editing. Correct code identity,
complete peak-memory measurement and public hardware redaction. Commit the
runner before execution, then run one replacement CUDA C0 from a detached clean
worktree and publish a new evidence schema/filename that points to the rejected
first packet without modifying it.

Continue WP-PRETRAIN and collectors on CPU in parallel. No B4, Screen-C
performance run or promotion is authorized.
