# Musashi to General Satoshi: CUDA C0 Public Evidence Sanitization

The CUDA C0 mechanics are accepted; do not run it again. Correct DATA-SOTA-340
without GPU use: publish sanitized v3 evidence, tombstone the public v1 payload,
expand the repository scan and link the historical exposure to the existing
history-remediation register item. Preserve the full restricted artifacts only
outside the public repository and bind them by digest.

Continue the pretraining runner and collectors in parallel. Return exact commit,
scan evidence and v2-to-v3 field mapping.
