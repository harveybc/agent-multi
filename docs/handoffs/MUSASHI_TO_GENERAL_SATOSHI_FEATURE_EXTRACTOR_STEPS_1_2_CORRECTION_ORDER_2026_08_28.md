# Musashi to General Satoshi: correction order for extractor steps 1 and 2

Date: 2026-08-28  
Source audit: `AUDIT_SATOSHI_FEATURE_EXTRACTOR_STEPS_1_2_2026_08_28.md`  
Priority: immediate

## Execution order

1. Preserve the delivered PRE/POST evidence and supersede rather than rewrite it.
2. Make the authoritative close stream economically complete: closure identity, side, size, entry/exit, gross PnL, costs, net PnL, source and reason.
3. Derive every authoritative trade statistic from that stream and move analyzer-only facts under an explicit diagnostic namespace.
4. Refuse mixed-population summaries and update all executing consumers, reports and OLAP-facing records.
5. Add conservation, duplicate-event and direct-settlement win/loss regressions through the real pipeline.
6. Run a bounded CPU active path, then one bounded CUDA active path with both a lifecycle close and a direct settlement. Zero-trade CUDA evidence is insufficient for this correction.
7. Replace the architecture manifest generator so runtime facts, module-declared contracts and literature comparisons are separate typed sections.
8. Persist actual layer-by-layer hooks, introspected topology and empirical temporal influence for every bar.
9. Regenerate the manifest and publish a field-level correction map from the rejected version.
10. Return focused tests, full suites, commands, raw facts and a request for independent reproduction.

## Redispatch condition

The paired SAC campaign may be proposed for fresh-attempt redispatch only when:

- all trade conservation identities hold in active CPU and CUDA paths;
- no executing consumer mixes analyzer subset statistics with authoritative totals;
- the corrected architecture manifest agrees with the exact module code and digests;
- Musashi independently reproduces the focused package.

Continue CPU implementation of the temporal-information suite in parallel. Do not wait idly for the redispatch decision.
