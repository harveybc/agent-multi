# Order to General Satoshi III: L1 Result, WP-B Corrections and Mechanism Ladder

Date: 2026-08-10 America/Bogota  
From: General Musashi, independent auditor  
To: General Satoshi III, technical lead  
Basis: `AUDIT_L1_FINAL_RESULT_AND_REPOSITORY_PRESENTATION_2026_08_10.md`

## Mission

Accept the reproduced L1 null result without turning it into doctrine, correct
findings 209-220, and restore useful GPU work by executing the already-approved
bounded M0-to-L1 mechanistic inspection. No new owner phrase is required.

Do not modify the sealed collection, its replica or the published aggregation.
Do not rewrite Git history. Do not clear the IBKR hold. Do not open the sealed
2025 split.

## WP0: Preserve the Accepted Result

1. Preserve experiment `2de49ea9225e2baf`, collection-manifest SHA-256
   `54447a20c864c3ccd1a0d81df6f052a5d5400c3c7c0bb2643f8646d69a72777f`
   and tree/replica digest
   `f3bb41516f8f3bb9b458c345aae3c1f261cc9688bece697cadc898d60401d374`.
2. Record the result as `INCONCLUSIVE / total activity collapse`: 16 valid,
   zero active, 16 unavailable Sharpes. Never relabel it as easy harmful or LR
   only.
3. Add one status-history row with start/end/duration, 16/16, terminal outcome,
   artifact pointer and zero active/queued workers after completion.
4. Keep every raw cell record and model artifact private; publish hashes,
   units, counts and the typed result only.

## WP1: Complete Corrections 209-216

Execute the prior round-2 order verbatim. Priority order:

1. 209-211 blockchain typed metadata, verified-history append cursor and
   explicit fleet chain identity;
2. 212-214 current-cell status, parallel critical-path ETA and live IBKR held
   state;
3. 216 Predictor no-header production fix;
4. 215 pre-push sensitivity gate plus a history-scrub plan. The repository is
   already private by owner authorization; do not rewrite history without a
   second explicit authorization.

Return before/after counterexamples and full suites. Do not deploy v2 chain
code until Musashi independently verifies 209-211.

## WP2: Correct WP-B Findings 217-219

1. Resolve all eight broken relative README links against exact Git trees.
   Generated/private outputs become inline code paths unless a small safe
   inspectable sample is deliberately committed.
2. Replace `superseded-by-doin-node` with the true replacement topic on
   `trading-signal` and `timeseries-gan`; retain exactly 20 accurate topics.
3. Refresh `causal-inference` presentation from a clean temporary worktree
   based on `origin/master`. Do not touch, stage, reset or copy over the dirty
   canonical worktree. Explicitly disclose that the package metadata still
   identifies an inherited `rl-optimizer` package until code ownership is
   separately repaired.
4. Re-run a checker that resolves every relative README target through
   `git cat-file -e <commit>:<target>`. Record external links separately; do
   not equate syntactic parsing with reachability.
5. Update inventory/snapshots for the owner-authorized private visibility of
   `agent-multi` without rewriting the historical before-state.

## WP3: Bounded M0-to-L1 Mechanism Ladder (Finding 220)

### 3.1 Question

Identify the first controlled M0-to-L1 change that turns an activity-preserving
policy into deterministic hold. This is diagnosis, not model selection.

### 3.2 Immutable common facts

Materialize one new diagnostic identity. Every training arm must bind the same:

- ETHUSD 4h data revision and the exact nested non-test splits used by L1;
- one seed, initially 101;
- mature source-anchor artifact and SHA-256;
- SAC topology, observation schema, feature order and scaling state;
- total gradient-update budget through the compared checkpoint;
- replay/optimizer carry semantics except in the arm explicitly testing the
  phase boundary;
- evaluation under normal-realistic dynamics with native SL and TP;
- exact code/environment/GPU identity and result schema.

No arm may see sealed 2025. No result may be promoted to live authority.

### 3.3 Arms

Run four training arms concurrently, one per GPU:

1. `D0_M0_EXACT`: exact executable reproduction of the previously active M0
   arm. This is the positive control.
2. `D2_BOUNDARY_ONLY`: D0 plus only the L1 easy-to-normal boundary/reload
   mechanism. Keep D0 costs, activity floor and stopping behavior.
3. `D3_COST_PROTECTION`: D2 plus only L1 normal costs and protected-entry
   semantics. Keep the D0 stopping rule.
4. `D4_FULL_L1`: D3 plus the L1 activity floor, patience and stopping rule.

After D0 terminates, execute `D1_EVALUATOR_ONLY` CPU-side using the exact D0
terminal artifact and trajectories: apply only the L1 paired activity
classifier to the same facts. D1 must not train or alter weights.

This ordering isolates: reproducibility, measurement gate, phase transition,
execution realism and stopper. Do not add LR levels, seeds, topologies,
features or order-type genes to this diagnostic.

### 3.4 Fail-closed interpretation

- If D0 fails to reproduce activity, the ladder is invalid. Diagnose source
  anchor, data/code revision and M0 claim before any new compute.
- If D0 is active and D1 changes the label, the defect is the activity
  definition, not learning.
- Otherwise, the first D2/D3/D4 transition from active to inactive names the
  mechanism requiring the next focused experiment.
- If all D0-D4 remain active, the prior collapse came from an uncontrolled
  identity difference; diff every bound manifest field.
- One seed is sufficient only to locate a deterministic mechanism. Any later
  decision about superiority requires paired replication.

### 3.5 Runtime and evidence

1. Unit/property tests must prove one-and-only-one intended delta between
   adjacent arm manifests.
2. Dispatch D0/D2/D3/D4 through the durable four-worker launcher with unique
   claims and fresh heartbeats. Reuse no L1 decision identity.
3. Report active job, current arm, epoch/activity/trades, GPU temperature and
   critical-path ETA from fresh per-attempt facts.
4. Seal and replicate terminal artifacts before interpretation.
5. Return a five-row contrast table with raw actions, non-hold rate, protected
   entries, trades by split, updates, elapsed time and artifact hashes.

## WP4: Remaining Fronts

1. Run Front-3 failed-enrichment retries through the existing idempotent,
   token-reserved path. This is not owner-blocked. Return aggregate counts and
   private pointers only.
2. Enrich the MT5 heartbeat with direct `account_binding_verified` evidence.
3. Prepare a fresh IBKR flat reconciliation and one-time hold-clear packet for
   the owner; keep the hold set until explicit authenticated action.
4. Preserve all legacy blockchains read-only. Prepare the explicit-ID v2
   migration manifest, but do not launch it before 209-211 acceptance and a
   DOIN job boundary.

## Acceptance

- 209-220 each has direct before/after evidence or remains honestly open.
- L1 seal and replica are unchanged.
- WP-B has zero broken relative links and accurate 20-topic sets.
- D0-D4 have one proven delta per adjacent pair and one diagnostic identity.
- Four GPUs run the four training arms with no duplicate claims.
- No broad sweep, live promotion, history rewrite, IBKR hold clear or v2 chain
  deployment occurs under this order.
