# Musashi to General Satoshi: M4 C17-C24 intervention foundation

Date: 2026-09-09

Priority: P0 CPU work in the model-capacity worktree. Start immediately.

Authority basis:

- owner instruction to continue the data-centric and model-capacity program;
- candidate tip `318cfdb8da0721d46f6da734c7c51659ad86dd4d`;
- sealed mechanics design v3 identity
  `cf4f429327933037531716c47c7dd6d036c020de0845689821dd427b2fcae013`;
- independent Musashi reproduction: committed C9-C16 POST passes and the
  focal M4 battery is 19/19 green; and
- M3 remains accepted and immutable. Do not regenerate it.

This order authorizes design, implementation, developmental calibration and a
bounded CPU mechanics run. It does not authorize confirmation outcomes, DOIN
genes, production gates, GPU use, financial data, venue access or alteration
of B4/T2. B4 remains read-only and T2 execution is handled by Musashi.

## 1. Recorded disposition

`M4_V3_RECONSTRUCTIBLE_MECHANICS_ACCEPTED`

The cumulative endpoint, 50:50 rehearsal, two-evaluation retention rule,
fresh-process continuation, matched 16-example diagnostic and independent
replay verifier are accepted as the mechanical foundation. Preserve v1-v3 and
the preflight evidence byte for byte.

The next scientific question is narrower than "capacity in bits":

> Conditional on a learned structured task, architecture and checkpoint, how
> many blinded random associations remain jointly acquired before the frozen
> retention rule fails, and do training measurements improve prediction of
> that endpoint beyond parameter count and checkpoint loss?

The endpoint is an empirical intervention result under this protocol. Never
call it unused intelligence, free bits, exact information content or exact
Kolmogorov complexity.

## 2. M4-C17: exact population and roles before scores

Create a machine-readable successor design that supersedes v3 without changing
its accepted mechanics. Before running any developmental score, enumerate the
complete candidate population and publish its digest.

The census must distinguish:

- structured Boolean families: identity, majority, DNF-3 and parity-4;
- the random-label task as a negative control, never as a learnable structured
  family;
- temporal families: sine, chirp, amplitude modulation, discontinuity and
  state-space;
- clean and applicable white, colored, impulsive and heteroscedastic noise
  regimes; the design must state which Cartesian products are meaningful and
  must not silently manufacture nonsensical Boolean/noise combinations;
- hidden widths 8, 16, 32 and 64;
- initialization, pre-stop, calibration-stop and bounded post-stop
  checkpoints; and
- disjoint DEVELOPMENT, CALIBRATION and CONFIRMATION generator identities.

For every candidate cell, record task family, noise regime or `NOT_APPLICABLE`,
width, checkpoint, generator role, generator id, model seed, optimizer budget
and the exact treatment/control/diagnostic roles. A generator is the
independent unit; model seeds and checkpoints are nested repetitions.

Do not assume the full Cartesian product is affordable. Compute the exact unit
and update census first. A deterministic reduction is allowed only by a rule
written before developmental outcomes, preserving every primary family,
width and checkpoint needed by the hypotheses.

## 3. M4-C18: freeze estimands and attribution

Define the primary endpoint exactly as the cumulative count of associations
jointly acquired at the last passing evaluation. Publish both stopping causes:
acquisition failure and two consecutive retention failures. `MAX_BATCHES` is
right-censored, not evidence that the endpoint equals the cap.

Freeze three analyses:

1. **Intervention effect:** within-generator paired difference between the
   learned checkpoint and matched random initialization under identical random
   association batches and compute.
2. **Checkpoint effect:** paired contrasts among the four checkpoints within
   generator, width and model seed. Do not treat checkpoints as independent.
3. **Incremental prediction:** out-of-generator prediction of `log1p(endpoint)`
   comparing, in this order, parameter count; parameter count plus checkpoint
   loss/elapsed updates; and those controls plus the frozen trajectory and
   description measurements. The full set advances only if it improves unseen-
   generator error and calibration after its measurement cost is included.

Compressed length, pruning and spectral rank are candidate descriptors, not
ground truth. Repeated weights do not imply remaining capacity. Preserve the
M3 result as prior evidence, not as a training sample for M4.

## 4. M4-C19: deterministic generator bank

Implement one canonical generator API that emits arrays and a manifest from
the sealed identity. It must provide:

- exact schemas, primitive types, dimensions, sampling rates and target rules;
- deterministic bytes for the same generator id;
- disjoint identities and bytes across DEVELOPMENT, CALIBRATION and
  CONFIRMATION;
- train-only fitting for every scale, threshold and noise parameter;
- an explicit clean latent signal and realized disturbance for synthetic
  temporal tasks, so empirical signal/disturbance measurements can be checked;
  and
- strict refusal of duplicate ids, role overlap, nonfinite values, mislabeled
  sampling frequency and future-dependent preprocessing.

Persist source arrays outside Git if large; commit manifests, schemas, summary
statistics and small adversarial fixtures. A digest declared by the producer
is not enough: the consumer recomputes it from the bytes used.

## 5. M4-C20: optimizer learnability gate

Before any residual-capacity interpretation, run a DEVELOPMENT-only
learnability screen with the exact optimizer and update budget proposed for the
intervention.

For each structured family and width, freeze a task-appropriate held-out
criterion and a simple baseline before scoring. Thresholds may be estimated on
DEVELOPMENT and frozen on CALIBRATION; CONFIRMATION remains untouched.

Required outcomes are:

- `LEARNABLE_UNDER_FROZEN_BUDGET`;
- `OPTIMIZATION_LIMITED`; or
- `NUMERICALLY_INVALID`.

Only the first may enter the later capacity intervention. Missing or failed
cells remain in the denominator. If a primary family fails at every width, the
design must report that limitation and stop interpreting it; do not increase
budgets after seeing confirmation behavior.

Include at least random initialization, a no-learning control and a deliberately
easy positive control. The random-label task validates that the gate does not
mislabel memorization as structured generalization.

## 6. M4-C21: precision, multiplicity and censoring

Before intervention outcomes, simulate or analytically derive the minimum
number of independent CONFIRMATION generators needed for the primary paired
effect at a stated smallest effect of interest. Publish assumptions, power or
precision target, attrition allowance and the exact family of confirmatory
contrasts.

Use generator-level resampling or a mixed model that keeps model seeds and
checkpoints nested. Correct the frozen confirmatory family for multiplicity.
Report family-specific effects; pool only under a predeclared heterogeneity
rule. Preserve failed, optimization-limited and censored units with typed
states. Define the analysis of right-censored `MAX_BATCHES` before it occurs.

If the available generator count cannot support the stated precision, return
`M4_CONFIRMATORY_POPULATION_INSUFFICIENT`; do not weaken the question.

## 7. M4-C22: one bounded runner and reconstructible custody

Build one config-driven CPU runner for the developmental screen and future
intervention. All scientific and resource values come from the sealed design;
CLI flags select only design, output root and `plan`/`execute` mode.

The runner must preserve the accepted v3 properties and additionally provide:

- a pre-result ledger containing every unit and role;
- deterministic scheduling independent of outcomes;
- private, write-once artifacts and typed terminals;
- durable resume that re-verifies every completed unit;
- executable wall, RSS, stop-file and heartbeat limits;
- exact accounting of optimization, evaluation and descriptor costs; and
- a fresh verifier that reconstructs generators, checkpoints, association
  batches, transitions, endpoint, censoring and model comparisons from raw
  persisted evidence.

No producer `passed`, `learnable`, `endpoint` or `verified` field may control a
verdict without re-derivation.

## 8. M4-C23: adversarial battery and bounded execution

Freeze regressions for at least:

1. DEVELOPMENT/CALIBRATION/CONFIRMATION generator overlap;
2. future-fitted scaling or noise calibration;
3. random-label null licensed as structured learning;
4. one forgotten early association hidden by the latest batch;
5. one retention failure counted as two;
6. `MAX_BATCHES` treated as an observed endpoint;
7. checkpoints or seeds treated as independent generators;
8. a missing/failed unit removed from the denominator;
9. descriptor cost omitted from the predictive comparison;
10. an unlearnable family relabeled capacity-limited;
11. a modified result with repaired producer digests; and
12. a second invocation overwriting any prior artifact.

Every bypass mutation must bite the productive verifier. Then execute only:

- the full DEVELOPMENT learnability screen on CPU; and
- at most four end-to-end DEVELOPMENT intervention units chosen by a rule in
  the sealed design, solely to measure runtime and validate mechanics.

Do not inspect CALIBRATION or CONFIRMATION outcomes. Do not tune the final
protocol from those four intervention endpoints.

## 9. M4-C24: return boundary

Return one packet with exact PRE/POST evidence, commit chain, final pushed tip,
population and resource census, precision calculation, developmental
learnability table, four-unit mechanics timings, adversarial/mutation results
and final-suite counts from the final tip.

Stop with exactly one of:

- `M4_INTERVENTION_DESIGN_AND_DEVELOPMENT_GATE_READY_FOR_MUSASHI_REVIEW`;
- `M4_OPTIMIZER_OR_POPULATION_LIMITATION_IDENTIFIED`; or
- `M4_CONFIRMATORY_POPULATION_INSUFFICIENT`.

Do not run the confirmatory intervention, author external review authority,
create a DOIN gene, use GPU, alter B4/T2/M3, inspect sealed financial data or
claim exact capacity in bits.

## 10. Parallel status duty

Observe B4 read-only once at start and once at return: service state, restart
count, completed/current cell, wall and thermal telemetry. Never stop, restart,
attach to or modify it. T2 is a Musashi-owned launch and is not a blocker for
this M4 order.
