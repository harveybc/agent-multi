# Musashi to General Satoshi: correct 313/314, then dispatch

Date: 2026-08-23
Priority: P1 decision campaign first; MT5 USDCAD second; grouped extractor CPU
work may continue in parallel.

## WP1 - P1 evidence binding

1. Reproduce finding 313 before editing.
2. Correct `outer_endpoint()` to bind `scored_rows` and `csv_sha256` from the
   real nested manifest and independently hash the CSV immediately before use.
3. Reject null, malformed, missing or changed evidence.
4. Add adversarial tests and regenerate the three smoke endpoint records from
   their immutable selected bundles when possible.
5. Return populated row counts and hashes plus the 27 focused tests and full
   suite result.

Acceptance of this correction authorizes immediate dispatch of the predeclared
4 seeds x 3 arms campaign without another owner phrase. Do not use sealed 2025.
Publish launch identities, host/GPU mapping and an ETA derived from the first
completed epoch. Mark non-divergent easy treatments uninformative.

## WP2 - MT5 local-secret materialization

1. Reproduce finding 314 with a destination symlink fixture.
2. Implement no-follow, atomic 0600 output under an enforced 0700 directory,
   with file and parent-directory fsync and fail-closed errors.
3. Add race, symlink, existing-directory and permission fixtures.
4. Re-run the 70 focused tests and the full LTS suite.

After independent acceptance, prepare but do not improvise the coordinated
operator window: compile updated EA, attach ETHUSD and USDCAD with distinct
magics, enable `require_route_identity`, capture signed bars, run preflight,
then activate only the USDCAD runner. Roll back USDCAD without disturbing ETH.

## WP3 - Parallel useful work

Continue grouped extractor sections 2-6 on CPU in a separate branch. Do not
mix its architecture into the flat-MLP P1 experiment.

Return one packet with exact commits, reproducer before/after output, tests,
remaining doubts, and current runtime facts. Close no finding yourself.
