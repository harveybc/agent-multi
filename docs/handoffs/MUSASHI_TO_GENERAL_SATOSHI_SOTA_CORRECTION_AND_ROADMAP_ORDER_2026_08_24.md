# Musashi to General Satoshi: SOTA Correction and Roadmap Order

Date: 2026-08-24
Authority: owner-approved research program; this order does not mutate the
running P1 campaign.

Read first:

1. `docs/audits/AUDIT_SATOSHI_SOTA_TRADING_AND_ROADMAP_IMPACT_2026_08_24.md`
2. `docs/research/sota_trading/00_INDEX.md`
3. `docs/research/sota_trading/08_TRAINING_OPTIMIZATION.md`
4. `docs/work_plan/38_NEAT_LESSONS_L1_L2_CURRICULUM_AND_FEATURE_SELECTION.md`

## Priority 0 — Preserve Runtime

- Do not alter, restart or reinterpret the running P1 4x3 campaign.
- Continue monitoring and publish terminal facts as already contracted.
- All SOTA corrections occur on the documentation branch.

## WP1 — Make the Review Reproducible

1. Fix the index count and include document 08.
2. Add a machine-readable source registry and rendered bibliography.
3. Give every quantitative claim a source ID and page/table/equation locator.
4. Separate paper, appendix, official code and secondary reproduction evidence.
5. Downgrade unsupported absence claims to `not_reported_in_reviewed_sources`.

Acceptance: a script rejects unknown source IDs, missing locators for numeric
claims, duplicate IDs and non-primary sources represented as primary.

## WP2 — Build a Decision-Oriented Gap Matrix

Add focused primary-source coverage for action representation, constrained and
risk-sensitive RL, recurrent/offline RL, execution uncertainty, walk-forward
adaptation and multiple-trial selection. For each open program decision record:

- candidate mechanism;
- evidence for and against;
- data/timeframe similarity to ETH H4;
- required inputs unavailable to us;
- computational cost;
- cheapest falsification experiment;
- collision with current work.

Do not call a mechanism SOTA without defining the decision and comparator.

## WP3 — Amend the Work Plan, Without Dispatching GPU Work

1. Relabel 2024 as development/research validation at program level.
2. Preserve 2025 as the only sealed test for this lineage.
3. Insert the action-contract screen before grouped-extractor optimization.
4. Insert rolling retraining cadence before architecture expansion.
5. Require capacity-matched architecture baselines and complexity reporting.
6. Keep portfolio/multi-asset work after the single-asset contract is sound.

Submit exact diffs and a dependency graph. Do not launch the new experiments
until P1 is terminal and Musashi verifies the designs.

## WP4 — Prepare Post-P1 Designs

Prepare, but do not launch:

- action screen: sign target, continuous target exposure, ternary
  deadband/hysteresis, explicit close/hold;
- retraining screen: frozen versus 168h/24h/12h adaptation;
- architecture screen: flat MLP versus small shared temporal baseline versus
  capacity-matched grouped extractor.

All designs must use rolling development origins, paired seeds, full costs,
turnover, drawdown, activity, inference latency and deadline evidence. State
how early closing and native SL/TP coexist. The sealed 2025 split must be absent
from every materialized development config.

## Return Packet

Return one packet with commit IDs, corrected index, source-registry validation,
gap matrix, work-plan diff and three non-launched experiment specifications.
List disagreements explicitly. This audit welcomes a technically supported
counterproposal; it does not welcome silent scope substitution.

