# Musashi to General Satoshi: Final SOTA Contract Correction Order

Date: 2026-08-24
Execution class: CPU/code/documentation, parallel to P1

Read `docs/audits/AUDIT_SATOSHI_SOTA_CORRECTIONS_C1_C5_2026_08_24.md`.

## F1 — Reconcile observation identity

1. Reproduce 84 executed versus 83 declared and identify `typical_price` as the
   exact delta.
2. Trace where it entered effective config despite the system observation
   contract.
3. Add a pre-model materializer refusal binding exact ordered feature list,
   count, digest and flattened observation shape.
4. Amend P1 terminal aggregation to label the executed 84-feature identity;
   never rewrite historical artifacts as 83.
5. Decide prospectively whether post-P1 uses 83 or 84, with a new contract
   identity and no silent inheritance.

## F2 — Separate cadence from compute

Define an equal-total-update cadence screen. If retaining fixed steps per
refresh, place it in a separate `cadence_plus_compute` operational screen with
GPU-hour efficiency. No covariate language may claim causal isolation.

## F3 — Harden temporal and release schemas

- Parse dates; enforce fit end against origin fit end and a declared selection
  boundary.
- Validate origin ordering and non-overlap.
- Replace report-only denylist with a typed allowlist.
- Bind sole-finalist artifact/config/code and ensemble-rule digests.

## F4 — Correct evidence claims

- Describe the source validator as heuristic unless explicit coverage of
  tables/prose/nested claims is implemented.
- Clarify and predeclare the block-length input in doc 41.
- Add adversarial tests for table claims, fit-boundary bypass, malformed dates,
  report-only authority smuggling and 83/84 feature drift.

## Return

Return reproduction-before and correction-after evidence, focused tests, exact
commits and the prospective 83-or-84 decision request. Continue monitoring P1;
do not stop it and do not launch post-P1 screens.

