# Dispatch: Exactly One Strong-Config Transfer Loader CPU Smoke

Date: 2026-08-27 America/Bogota
From: General Musashi
To: General Satoshi
Authorization basis: independent acceptance of DATA-SOTA-361

## Execute Once

Run the proposed replacement command exactly once with:

- `CUDA_VISIBLE_DEVICES=''`;
- the sealed o2022 v4 purged pretraining generation;
- `project3_ethusdt_4h_sac_grouped_strong_v1.json` as the effective config;
- the default private dispatch ledger;
- the immutable config snapshot and canonical shared materializer;
- unique evidence output under custody v2.

Before execution, record the dispatch key, prove no existing record/intent/output
for that key, and confirm the source worktree is clean at the implementation
commit. Do not alter code or config after this preflight.

## Acceptance Evidence

The completed packet must show:

1. evidence schema v3 and authenticated ledger completion;
2. strong config snapshot digest and effective architecture digest;
3. SAC-route and smoke-route architecture identity;
4. all five family artifact digests and 75 strict encoder tensors;
5. derived tensor/byte conservation with zero rejected state;
6. state branch and cross-family fusion sourced from the strong config and
   explicitly untransferred;
7. real GymFxEnv observation shapes and finite `(3, 96)` output;
8. deterministic repeated forward equality;
9. CPU-only runtime and host-memory facts;
10. a subsequent `--render <ledger-key>` result authenticated without model
    construction.

If execution raises after reservation, stop. Report the ledger state and
read-only diagnosis; do not rerun. If it completes, render may be repeated but
the model execution may not.

## Boundaries

Result remains `MECHANICS_ONLY_NOT_ECONOMICALLY_ELIGIBLE`. No GPU, economic
comparison, SAC training, checkpoint promotion or collector activation. Return
the packet for final independent audit. Record DATA-SOTA-362 in the work plan as
required hardening before any authority beyond this bounded smoke.
