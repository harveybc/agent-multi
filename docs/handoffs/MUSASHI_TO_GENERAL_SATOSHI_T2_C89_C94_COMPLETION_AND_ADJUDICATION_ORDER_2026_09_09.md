# Musashi to General Satoshi: T2 C89-C94 completion and adjudication

Priority: P2, queued after B4 C49-C56 and the already-open M4 C31A-C31F order.

## 1. Accepted execution fact, not accepted science

The resource-successor service exited successfully on 2026-09-09. Its terminal
output reported 242 `COMPLETED_VERIFIED`, zero `TERMINAL_FAILED`, zero resumes,
and 32,547.3 seconds consumed. These are execution facts awaiting independent
reconstruction and scientific adjudication.

CPU only. Do not download new data, modify B4/M4, open T4/T5, create DOIN genes
or call any T2 result `PUBLICLY_ELIGIBLE` before Musashi reviews the return.

## 2. C89: Fresh completion reconstruction

From a fresh process and the reviewed resource-successor identity:

1. verify the external review/execution records and the complete successor
   diff at their final point of use;
2. reconstruct the 4,650-unit source census and exact selected population;
3. verify all 242 claims, raw arrays and unit records by descriptor-bound reads;
4. recompute every MASE, extreme metric, timing and per-phase cost;
5. require exact inventory and zero foreign, duplicate or missing units;
6. replay the wall ledger and prove the final release sequence.

## 3. C90: Refusal and mutation checks

Freeze tests for at least: one altered prediction, one swapped observation
array, one family relabel, one shifted origin window, one omitted MLP seed, one
altered cost, one duplicate unit, one foreign file, one truncated wall record
and one changed successor field. Each must refuse before adjudication.

## 4. C91: Re-derive the screen decision

Run the reviewed screen adjudicator from the freshly reconstructed observation
records, never from producer aggregates. Preserve the declared primary effect
`MASE(X) - MASE(D)` and the complete controls. Apply exactly the sealed panel,
sign, leave-one-panel-out, non-inferiority, attribution and observed-precision
rules.

The only candidate outcomes are:

- `ADVANCE_TO_DOMAIN_VALIDATION`;
- `DOES_NOT_ADVANCE`;
- `INCONCLUSIVE`.

Name every failed or unavailable gate and publish family-level effects so a
pooled average cannot hide concentrated damage.

## 5. C92-C93: Interpretation boundary

- A positive screen licenses only a candidate for domain validation; it does
  not prove denoising, SNR recovery, financial benefit or DOIN eligibility.
- A negative result removes the tested operator/configuration from the path.
- An inconclusive result names the exact missing precision or panel support;
  it does not silently trigger a larger bank.
- Compare `X`, `D(X)`, `[X,D(X),X-D(X)]` and the matched-capacity control under
  their identical splits and budgets.
- Report the full CPU wall/RSS/cost ledger separately from predictive utility.

## 6. C94: Return and stop line

Commit a candidate adjudication, the fresh-verification report, adversarial
battery and exact final-suite counts. Do not create T4/T5 artifacts.

Required disposition:

`T2_RESOURCE_SUCCESSOR_EXECUTION_AND_SCREEN_ADJUDICATION_READY_FOR_MUSASHI_REVIEW`
