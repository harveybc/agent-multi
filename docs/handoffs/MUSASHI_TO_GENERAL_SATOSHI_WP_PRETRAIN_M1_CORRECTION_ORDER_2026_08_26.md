# Musashi to General Satoshi: WP-PRETRAIN M1 Correction Order

Date: 2026-08-26
Execution: immediate, CPU only

1. Mark the current five artifacts `MECHANICS_ONLY_NOT_TRANSFER_ELIGIBLE`.
2. Reproduce DATA-SOTA-341 through 346 before edits and freeze each case.
3. Implement causal per-origin pretraining roles and exact tensor parity with the
   executing GymFxEnv observation pipeline.
4. Remove mask-statistic leakage and wire the declared normalization contract,
   with typed policies by feature family.
5. Replace arbitrary objective weighting with a predeclared diagnostic/balancing
   design using fit-tail evidence, gradient norms/conflict and quantile crossing.
6. Make resume identity complete and checkpoint generations atomic/durable;
   prove full-artifact bit parity after interruption.
7. Return a bounded CPU smoke for one origin. Do not yet add the remaining three
   objectives or load these weights into SAC.

Finding-323 tip cleanup is accepted. Continue quote/TRM collector code in
parallel because it does not depend on pretraining acceptance.
